name: C
on: [push, pull_request]
jobs:

  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:

    - name: Set up environment
      run: sudo apt install -y libmap0g make

    - name: Check out code
      uses: actions/checkout@v3

    - name: Build
      run: make
